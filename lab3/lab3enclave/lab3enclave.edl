/* описание импортируемых функция для анклава */
enclave {
    trusted { 
        /* define ECALLs here. */
        public void foo([out, size=len] char* buf, size_t len, size_t idx); /* объявление функции */
    };
};
